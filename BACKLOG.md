# LeadPilot AI CRM — Backlog & Roadmap

---

## Sprint v3.1.0 (Target Roadmap)
- Shared package extraction (`packages/shared` npm workspaces export).
- Turborepo / Nx build orchestration engine setup for caching tasks across `apps/web` and `apps/api`.
- Docker Compose local development container orchestration (`docker/docker-compose.yml`).
