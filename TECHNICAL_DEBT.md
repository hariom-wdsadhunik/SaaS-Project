# LeadPilot AI CRM — Technical Debt Audit & Registry

---

## Registered Items

1. **React Compiler Warnings:** React Hook Form `watch()` and TanStack Table `useReactTable()` trigger React Compiler memoization warnings. (Severity: Low, Non-blocking).
2. **Deprecation Notice:** Next.js `middleware.ts` deprecation warning recommends migrating to proxy convention in future release. (Severity: Low, Non-blocking).
