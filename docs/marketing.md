# LeadPilot AI CRM — Marketing Website Architecture

**Module:** Marketing & Public Presence  
**Version:** v2.1.0  

---

## 1. Structure & Shared Design System

The public marketing site shares design tokens (`src/styles/tokens.css`) and UI primitives (`Button`, `Card`, `Badge`) with the core CRM platform.

- **Pages Built:** Home (`/`), Features (`/features`), Pricing (`/pricing`).
- **Layout:** `MarketingNavbar` and `MarketingFooter`.
