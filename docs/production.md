# Production Architecture & Operations

- Observability stack (`HealthCheckService`, `MetricsCollector`, `MonitoringService`).
- Security Audit (`SECURITY_AUDIT.md`).
- Deployment & Runbook guides (`DeploymentChecklist.md`, `Runbook.md`).
