# LeadPilot AI CRM — Release v0.3.0 (Supabase Deal Repository Integration)

## 1. Feature & Architecture Summary

- **Supabase Deal Repository (`SupabaseDealRepository`):** Replaced mock Deal services with a production-grade Supabase repository implementing `DealRepository` contract (`src/contracts/deal/repository.ts`).
- **Database Schema & RLS:** Created `supabase/migrations/20260726104000_create_deals_table.sql` with strict authenticated RLS policies (NO `USING (true)` wildcards).
- **Kanban Board & Pipeline Persistence:** Stage transitions, probability scores, search queries, and modal CRUD operations persist directly to Supabase database with persistent client-side state sync.
- **AI Tooling Integration (`DealTool`):** Updated `DealTool` (`src/domain/ai/tools/DealTool.ts`) to fetch live deal strategy metrics directly from `SupabaseDealRepository`.
- **Telemetry Observability:** Integrated `platformAuditLogger` for deal events (`Deal Created`, `Deal Updated`, `Deal Deleted`, `Stage Changed`, `Owner Changed`).

## 2. Migration Notes

- Run Supabase migration script `20260726104000_create_deals_table.sql`.
- Environment requirements: `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.

## 3. Verification Results

- **ESLint Audit:** `0 errors`.
- **Next.js Production Build:** `18/18` static pages prerendered in 1094ms.
- **Unit Test Suite (`src/tests/unit/deal-repository.test.ts`):** All test cases **PASSED**.
