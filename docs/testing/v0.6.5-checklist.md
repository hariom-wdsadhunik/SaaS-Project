# LeadPilot AI CRM — v0.6.5 Platform Infrastructure Checklist

Use this QA checklist to verify the Platform Infrastructure features.

---

## QA Verification Matrix

| Test Suite / Feature | Verification Procedure | Expected Outcome | Status |
| :--- | :--- | :--- | :---: |
| **1. Supabase Realtime** | Execute `src/tests/unit/realtime.test.ts`. | Subscription registration, duplicate prevention, and payload dispatch pass. | [x] |
| **2. Event Bus** | Execute `src/tests/unit/event-bus.test.ts`. | Strongly typed events publish and dispatch to subscribers with isolated error handling. | [x] |
| **3. Notification Engine** | Execute `src/tests/unit/notification.test.ts`. | In-App notifications deliver, unread count updates, and preferences filter disabled channels. | [x] |
| **4. Background Jobs** | Execute `src/tests/unit/jobs.test.ts`. | Jobs enqueue, runner processes handlers, and retry policies calculate exponential backoff. | [x] |
| **5. Presence Service** | Execute `src/tests/unit/presence.test.ts`. | Online/Offline status transitions, last seen timestamps, and typing state updates. | [x] |
| **6. Audit Stream** | Trigger domain events on `EventBus`. | Audit records automatically captured in `AuditStream`. | [x] |
| **7. AI Event Hooks** | Publish `TaskCompleted` event. | `AIOrchestrator` logs automated follow-up recommendation hook. | [x] |
| **8. UI Notification Center**| Click notification bell in navbar. | Unread badge counts and notification drawer opens cleanly. | [x] |
