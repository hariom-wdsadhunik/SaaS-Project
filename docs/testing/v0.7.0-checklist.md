# LeadPilot AI CRM — v0.7.0 Omnichannel Communication Platform Checklist

Use this QA checklist to verify the Omnichannel Communication Platform features.

---

## QA Verification Matrix

| Test Case | Procedure | Expected Outcome | Status |
| :--- | :--- | :--- | :---: |
| **1. Communication Repository** | Execute `src/tests/unit/communication-repository.test.ts`. | Conversation creation, message sending, and archiving pass. | [x] |
| **2. Conversation Lifecycle** | Execute `src/tests/unit/conversation.test.ts`. | Inbound receiving and mark-as-read workflows pass. | [x] |
| **3. Provider Adapters** | Execute `src/tests/unit/provider-adapter.test.ts`. | `ProviderFactory` returns correct adapters for WhatsApp, Email, SMS. | [x] |
| **4. Notification Integration** | Execute `src/tests/unit/notification-integration.test.ts`. | Inbound message triggers in-app notification for assigned broker. | [x] |
| **5. API v1 Endpoints** | Execute `src/tests/unit/api-v1.test.ts`. | `/api/v1/*` routes return `v1` versioned JSON responses. | [x] |
| **6. Contact Timeline Auto-Append**| Send message via `SupabaseCommunicationRepository`. | Event automatically appended to `contact_timeline`. | [x] |
