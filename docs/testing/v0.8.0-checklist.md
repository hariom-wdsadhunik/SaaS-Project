# LeadPilot AI CRM — v0.8.0 Intelligent Document Management Checklist

Use this QA checklist to verify the Intelligent Document Management Platform features.

---

## QA Verification Matrix

| Test Case | Procedure | Expected Outcome | Status |
| :--- | :--- | :--- | :---: |
| **1. Document Repository** | Execute `src/tests/unit/document-repository.test.ts`. | Upload, download, rename, and fetch ops succeed. | [x] |
| **2. Storage Subsystem** | Execute `src/tests/unit/storage.test.ts`. | File size limits, extensions, SHA-256 hashes, and signed URLs pass. | [x] |
| **3. Permissions** | Execute `src/tests/unit/permissions.test.ts`. | Document sharing and permission granting pass. | [x] |
| **4. Upload Pipeline** | Execute `src/tests/unit/upload.test.ts`. | `DocumentUploader` calculates SHA-256 and writes storage paths. | [x] |
| **5. Search** | Execute `src/tests/unit/search.test.ts`. | Document search returns matching records. | [x] |
| **6. Versioning** | Execute `src/tests/unit/versioning.test.ts`. | Creating a new version increments version number. | [x] |
| **7. Contact Timeline Auto-Append**| Perform document operations via `SupabaseDocumentRepository`. | Events automatically appended to `contact_timeline`. | [x] |
| **8. API v1 Endpoints** | Query `/api/v1/documents`, `/api/v1/folders`, `/api/v1/uploads`, `/api/v1/shares`. | Routes return versioned `v1` JSON responses. | [x] |
