# LeadPilot AI CRM — v0.9.0 Analytics Platform Checklist

Use this QA checklist to verify the Analytics & Business Intelligence Platform features.

---

## QA Verification Matrix

| Test Case | Procedure | Expected Outcome | Status |
| :--- | :--- | :--- | :---: |
| **1. Analytics Foundation** | Execute `src/tests/unit/analytics.test.ts`. | `MetricRegistry` contains 11 definitions and `MetricCache` caches metrics. | [x] |
| **2. KPI Engine** | Execute `src/tests/unit/kpi-engine.test.ts`. | Calculates and returns 11 core KPIs with status scoring. | [x] |
| **3. Forecast Engine** | Execute `src/tests/unit/forecast.test.ts`. | Generates 30-day predictive forecast points and growth rate. | [x] |
| **4. Executive Dashboard** | Execute `src/tests/unit/dashboard.test.ts`. | Returns 9 executive dashboard widgets. | [x] |
| **5. Report Engine** | Execute `src/tests/unit/report.test.ts`. | Creates custom reports and exports to CSV format. | [x] |
| **6. AI Insights Engine** | Execute `src/tests/unit/insight.test.ts`. | Generates ranked AI insights with recommended actions. | [x] |
| **7. API v1 Endpoints** | Query `/api/v1/analytics`, `/api/v1/reports`, `/api/v1/dashboard`, `/api/v1/forecast`. | Routes return versioned `v1` JSON responses. | [x] |
