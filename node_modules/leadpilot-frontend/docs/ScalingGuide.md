# System Infrastructure Scaling Guide

- **Next.js Frontend:** Auto-scaling serverless edge deployment on Vercel.
- **Supabase PostgreSQL:** Read-replicas for analytical queries (`KPIEngine`).
- **Vector Search:** `pgvector` index tuning (`ivfflat` / `HNSW` indexes for fast RAG search).
